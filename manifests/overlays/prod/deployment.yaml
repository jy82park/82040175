apiVersion : apps/v1
kind: Deployment
metadata:
  name: "82040175"
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: "82040175"
          command: ["java","-jar","/app.jar"]
          args: ["--spring.config.location=file:/config/application.yml"]
          volumeMounts:
          - mountPath: /mnt/postgres
            name: "82040175-volume"
            readOnly: false
            subPath: postgres
          - mountPath: /config
            name: application-properties
            readOnly: true
                          
      volumes:
      - name: "82040175-volume"
        persistentVolumeClaim:
          claimName: ce-aa-pvc
      - name: application-properties
        configMap:
          name: "82040175-configmap"
          items:
            - key: application.yml
              path: application.yml
          defaultMode: 420
